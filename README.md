# SeOs

SeOs is a web based OS.

# Start up(backend)
1. cd backend
2. npm install || npm i
3. npm start

# .env Requirements(backend)
1. MONGO_URI
2. PORT
3. JWT_SECRET

# Start up(client)
1. cd client
2. npm i || npm install
3. npm start

# .env Requirements(client)
